# Airflow (latest 2.10.x - stable production release)
apache-airflow==2.10.4

# dbt with DuckDB adapter (verified compatible versions from dbt platform compatible track)
dbt-core==1.10.11
dbt-adapters==1.16.6
dbt-common==1.29.0
dbt-duckdb==1.9.6

# Astronomer Cosmos for dbt + Airflow integration (latest)
astronomer-cosmos==1.10.2

# Additional utilities
pandas>=2.2.3
pyarrow>=18.1.0
