apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    url: http://prometheus:9090
    isDefault: false
    editable: false
    jsonData:
      timeInterval: "30s"
